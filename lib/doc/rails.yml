setup:
  db_name: |
    Database name (rails).
    
    set :db_name, "app_db_name"
  
  db_user: |
    Database user (rails).
    
    set :db_user, "app_db_user"
    
  db_pass: |
    Database password (rails).
    
    set :db_pass, "the_password"